{
  "name": "dappdrop-subgraph",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:studio": "graph deploy --studio dappdrop",
    "deploy:hosted": "graph deploy --product hosted-service $GRAPH_HOSTED_SERVICE_NAME",
    "create:local": "graph create --node http://localhost:8020/ dappdrop",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dappdrop"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.91.0",
    "@graphprotocol/graph-ts": "^0.35.1"
  }
}
